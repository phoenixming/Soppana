using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stranger : NPC
{
    // Start is called before the first frame update
    void Start()
    {
        friendPointMulitiplier = 1;
        type = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
